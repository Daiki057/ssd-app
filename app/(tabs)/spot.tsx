import { View, Text } from 'react-native';

export default function SpotScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>スポット共有画面</Text>
    </View>
  );
}